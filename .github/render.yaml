services:
  - type: web
    name: mlb_betting
    env: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install --only-binary=:all: numpy==1.21.2
      pip install -r requirements.txt
    startCommand: gunicorn app:app  # or your start command
